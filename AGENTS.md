# Project Guidelines

## Icons

- Use `@phosphor-icons/react` for interface icons.
- Prefer an existing Phosphor icon over text glyphs, emoji, custom SVGs, or icons drawn with CSS.
- Keep icon weight and size consistent with the surrounding control; use `aria-hidden="true"` for decorative icons and an accessible label for icon-only controls.
